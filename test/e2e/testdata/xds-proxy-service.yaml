apiVersion: v1
kind: Service
metadata:
  name: envoy-hub-proxy
  labels:
    hub.mydecisive.ai/xds-proxy: "true"
spec:
  type: ClusterIP
  selector:
    app: envoy-hub-proxy
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
