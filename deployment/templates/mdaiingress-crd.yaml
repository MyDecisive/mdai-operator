{{- if .Values.crds.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mdaiingresses.hub.mydecisive.ai
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
  {{- include "deployment.labels" . | nindent 4 }}
spec:
  group: hub.mydecisive.ai
  names:
    kind: MdaiIngress
    listKind: MdaiIngressList
    plural: mdaiingresses
    singular: mdaiingress
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MdaiIngress is the Schema for the mdaiingresses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MdaiIngressSpec defines the desired state of MdaiIngress
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations to add to ingress.
                  e.g. 'cert-manager.io/cluster-issuer: "letsencrypt"'
                type: object
              cloudType:
                description: 'Supported types are: aws'
                enum:
                - aws
                type: string
              collectorEndpoints:
                additionalProperties:
                  type: string
                description: |-
                  CollectorEndpoints should contain dns names for all collectors endpoints (grpc receivers)
                  mapped to receivers names e.g. :
                  otlp/1: otlp1.grpc.some.domain
                  otlp/2: otlp2.grpc.some.domain
                type: object
              grpcService:
                description: IngressService defines Service properties related to Ingress
                  configuration.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to service.
                    type: object
                  type:
                    description: Type defines the type of the Service.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    - ExternalName
                    type: string
                required:
                - type
                type: object
              ingressClassName:
                description: |-
                  IngressClassName is the name of an IngressClass cluster resource. Ingress
                  controller implementations use this field to know whether they should be
                  serving this Ingress resource.
                type: string
              nonGrpcService:
                description: IngressService defines Service properties related to Ingress
                  configuration.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to service.
                    type: object
                  type:
                    description: Type defines the type of the Service.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    - ExternalName
                    type: string
                required:
                - type
                type: object
              otelCollector:
                description: Name of the Otel Collector, current Custom Resource belongs
                  to.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - cloudType
            - grpcService
            - nonGrpcService
            - otelCollector
            type: object
          status:
            description: MdaiIngressStatus defines the observed state of MdaiIngress
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}
