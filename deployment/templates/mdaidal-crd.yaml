{{- if .Values.crds.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mdaidals.hub.mydecisive.ai
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
  {{- include "deployment.labels" . | nindent 4 }}
spec:
  group: hub.mydecisive.ai
  names:
    kind: MdaiDal
    listKind: MdaiDalList
    plural: mdaidals
    singular: mdaidal
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MdaiDal is the Schema for the mdaidals API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MdaiDal
            properties:
              aws:
                properties:
                  credentials:
                    properties:
                      accessKeyField:
                        default: AWS_ACCESS_KEY_ID
                        description: |-
                          AccessKeyField is the key of the AWS credentials secret
                          that holds the value for `AWS_ACCESS_KEY_ID`
                        minLength: 1
                        type: string
                      secretKeyField:
                        default: AWS_SECRET_ACCESS_KEY
                        description: |-
                          SecretKeyField is the key of the AWS credentials secret
                          that holds the value for `AWS_SECRET_ACCESS_KEY`
                        minLength: 1
                        type: string
                      secretName:
                        default: aws-credentials
                        description: |-
                          SecretName is the name of the kubernetes secret that holds
                          the AWS credentials
                        minLength: 1
                        type: string
                    type: object
                  region:
                    description: Region is the AWS Region
                    pattern: ^[a-z]{2}(-[a-z]+)+-\d+$
                    type: string
                required:
                - region
                type: object
              granularity:
                description: Granularity is how often to partition the data
                enum:
                - weekly
                - daily
                - hourly
                - minutely
                type: string
              imageSpec:
                default:
                  pullPolicy: IfNotPresent
                  repository: public.ecr.aws/decisiveai/mdai-dal
                  tag: 0.0.1
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: PullPolicy, e.g. IfNotPresent, Always, Never
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  repository:
                    default: public.ecr.aws/decisiveai/mdai-dal
                    description: Repository, e.g. public.ecr.aws/decisiveai/mdai-dal
                    type: string
                  tag:
                    default: 0.0.1
                    description: Tag, e.g. v0.0.1
                    type: string
                type: object
              marshaler:
                default: json
                description: Marshaler of payload data
                type: string
              s3:
                properties:
                  bucket:
                    description: Bucket is the name of the S3 bucket to write to
                    maxLength: 63
                    minLength: 3
                    pattern: ^[a-z0-9][a-z0-9.-]*[a-z0-9]$
                    type: string
                  prefix:
                    description: Prefix is the S3 bucket prefix (path)
                    pattern: ^[A-Za-z0-9._/\-]*$
                    type: string
                required:
                - bucket
                type: object
              usePayloadTs:
                default: false
                description: |-
                  UsePayloadTS determines whether to partition by earliest payload
                  timestamp instead of ingest time
                type: boolean
            required:
            - aws
            - granularity
            - s3
            type: object
          status:
            description: status defines the observed state of MdaiDal
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: Endpoint is the in-cluster address for the DAL service.
                type: string
              lastSyncedTime:
                description: LastSyncedTime is the last time the controller successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation the controller
                  processed.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of ready replicas for the DAL
                  deployment.
                format: int32
                type: integer
              replicas:
                description: Replicas is the desired number of replicas for the DAL
                  deployment.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}
