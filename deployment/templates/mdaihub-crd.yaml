{{- if .Values.crds.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mdaihubs.hub.mydecisive.ai
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
  {{- include "deployment.labels" . | nindent 4 }}
spec:
  group: hub.mydecisive.ai
  names:
    kind: MdaiHub
    listKind: MdaiHubList
    plural: mdaihubs
    singular: mdaihub
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MdaiHub is the Schema for the mdaihubs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MdaiHubSpec defines the desired state of MdaiHub.
            properties:
              config:
                properties:
                  evaluation_interval:
                    description: EvaluationInterval Specify the interval at which all
                      evaluations within this hub are assessed in the Prometheus infrastructure.
                    pattern: ^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$
                    type: string
                type: object
              prometheusAlerts:
                items:
                  properties:
                    expr:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    for:
                      description: For Alerts are considered firing once they have been
                        returned for this long.
                      pattern: ^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$
                      type: string
                    keep_firing_for:
                      description: KeepFiringFor defines how long an alert will continue
                        firing after the condition that triggered it has cleared.
                      minLength: 1
                      pattern: ^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$
                      type: string
                    name:
                      description: Name How this evaluation will be referred to elsewhere
                        in the config. Also, the name applied to the Prometheus Alert
                      minLength: 1
                      pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                      type: string
                    severity:
                      pattern: ^(warning|critical)$
                      type: string
                  required:
                  - expr
                  - name
                  - severity
                  type: object
                type: array
              rules:
                items:
                  properties:
                    name:
                      description: Name How this rule will be referred to elsewhere
                        in the config and audit.
                      minLength: 1
                      type: string
                    then:
                      description: Then specifies the actions to be taken when the rule
                        is triggered.
                      items:
                        properties:
                          addToMap:
                            properties:
                              key:
                                description: Key to add or remove
                                minLength: 1
                                type: string
                              map:
                                description: Map Target map variable name
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              value:
                                description: Value for key. Is not required for remove
                                  operations.
                                type: string
                            required:
                            - key
                            - map
                            type: object
                          addToSet:
                            description: Payloads (one required depending on Type)
                            properties:
                              set:
                                description: Target set variable name
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              value:
                                description: Value to add (templated string allowed)
                                minLength: 1
                                type: string
                            required:
                            - set
                            - value
                            type: object
                          callWebhook:
                            description: CallWebhookAction is used to call a webhook
                              with the provided template.
                            properties:
                              headers:
                                additionalProperties:
                                  type: string
                                description: Headers additional HTTP headers to be included
                                  in the request.
                                type: object
                              headersFrom:
                                additionalProperties:
                                  description: ValueFromSource refers to a key in a
                                    Secret or ConfigMap.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key of
                                        a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret or
                                            its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                  x-kubernetes-validations:
                                  - message: set exactly one of secretKeyRef or configMapKeyRef
                                    rule: (has(self.secretKeyRef)?1:0)+(has(self.configMapKeyRef)?1:0)==1
                                description: HeadersFrom additional HTTP headers to
                                  be included in the request from a Secret or ConfigMap.
                                type: object
                              method:
                                default: POST
                                description: Method defaults to POST if not provided.
                                  If template is used, POST method has to be used.
                                enum:
                                - POST
                                - PUT
                                - PATCH
                                type: string
                              payloadTemplate:
                                description: |-
                                  PayloadTemplate A reusable JSON payload template stored inline or referenced from a ConfigMap/Secret.
                                  Used when TemplateRef is "jsonTemplate".
                                properties:
                                  value:
                                    type: string
                                  valueFrom:
                                    description: ValueFromSource refers to a key in
                                      a Secret or ConfigMap.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key from a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: SecretKeySelector selects a key
                                          of a Secret.
                                        properties:
                                          key:
                                            description: The key of the secret to select
                                              from.  Must be a valid secret key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                    x-kubernetes-validations:
                                    - message: set exactly one of secretKeyRef or configMapKeyRef
                                      rule: (has(self.secretKeyRef)?1:0)+(has(self.configMapKeyRef)?1:0)==1
                                type: object
                                x-kubernetes-validations:
                                - message: set exactly one of value or valueFrom
                                  rule: (has(self.value)?1:0)+(has(self.valueFrom)?1:0)==1
                              templateRef:
                                default: jsonTemplate
                                description: |-
                                  TemplateRef uses built-in templates or custom ones.
                                  `slackAlertTemplate` uses Slack's built-in alert template.
                                  `jsonTemplate` is default and uses a custom JSON payload template referenced from a ConfigMap/Secret.
                                enum:
                                - slackAlertTemplate
                                - jsonTemplate
                                type: string
                              templateValues:
                                additionalProperties:
                                  type: string
                                description: TemplateValues literal key/value pairs
                                  available to the template as ${template:KEY}.
                                type: object
                              templateValuesFrom:
                                additionalProperties:
                                  description: ValueFromSource refers to a key in a
                                    Secret or ConfigMap.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key of
                                        a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret or
                                            its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                                  x-kubernetes-validations:
                                  - message: set exactly one of secretKeyRef or configMapKeyRef
                                    rule: (has(self.secretKeyRef)?1:0)+(has(self.configMapKeyRef)?1:0)==1
                                description: TemplateValuesFrom sourced from Secrets/ConfigMaps
                                  (overridden by TemplateValues).
                                type: object
                              timeout:
                                description: |-
                                  Timeout specifies the maximum time to wait for the response from the webhook before retry.
                                  Default is 0s, means no timeout should be applied.
                                type: string
                              url:
                                description: URL provided as string or read from secret
                                  or configmap within hub namespace.
                                properties:
                                  value:
                                    type: string
                                  valueFrom:
                                    description: ValueFromSource refers to a key in
                                      a Secret or ConfigMap.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key from a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: SecretKeySelector selects a key
                                          of a Secret.
                                        properties:
                                          key:
                                            description: The key of the secret to select
                                              from.  Must be a valid secret key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                    x-kubernetes-validations:
                                    - message: set exactly one of secretKeyRef or configMapKeyRef
                                      rule: (has(self.secretKeyRef)?1:0)+(has(self.configMapKeyRef)?1:0)==1
                                type: object
                                x-kubernetes-validations:
                                - message: set exactly one of value or valueFrom
                                  rule: (has(self.value)?1:0)+(has(self.valueFrom)?1:0)==1
                            required:
                            - templateRef
                            - url
                            type: object
                          cleanUpReplay:
                            type: object
                          deployReplay:
                            properties:
                              replaySpec:
                                description: ReplaySpec is a MdaiReplaySpec CR spec
                                  defining the parameters of this replay
                                properties:
                                  destination:
                                    properties:
                                      otlpHttp:
                                        properties:
                                          endpoint:
                                            type: string
                                        required:
                                        - endpoint
                                        type: object
                                    type: object
                                  endTime:
                                    type: string
                                  hubName:
                                    type: string
                                  ignoreSendingQueue:
                                    default: false
                                    description: 'IgnoreSendingQueue: Bypass checking
                                      the OTEL sending queue metric when finalizing
                                      the replay resource'
                                    type: boolean
                                  opampEndpoint:
                                    type: string
                                  resource:
                                    properties:
                                      image:
                                        type: string
                                    type: object
                                  source:
                                    properties:
                                      aws:
                                        properties:
                                          awsAccessKeySecret:
                                            type: string
                                        type: object
                                      s3:
                                        properties:
                                          filePrefix:
                                            type: string
                                          s3Bucket:
                                            type: string
                                          s3Partition:
                                            enum:
                                            - minute
                                            - hour
                                            type: string
                                          s3Path:
                                            type: string
                                          s3Region:
                                            type: string
                                        required:
                                        - filePrefix
                                        - s3Bucket
                                        - s3Partition
                                        - s3Path
                                        - s3Region
                                        type: object
                                    type: object
                                  startTime:
                                    type: string
                                  statusVariableRef:
                                    description: |-
                                      StatusVariableRef a variable that will be updated with the result of this replay upon success or failure.
                                      Should be referenced in a When VariableUpdated rule so that a CleanUpReplayAction can be triggered
                                    type: string
                                  telemetryType:
                                    enum:
                                    - logs
                                    - metrics
                                    - traces
                                    type: string
                                required:
                                - opampEndpoint
                                - statusVariableRef
                                type: object
                            required:
                            - replaySpec
                            type: object
                          removeFromMap:
                            properties:
                              key:
                                description: Key to add or remove
                                minLength: 1
                                type: string
                              map:
                                description: Map Target map variable name
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              value:
                                description: Value for key. Is not required for remove
                                  operations.
                                type: string
                            required:
                            - key
                            - map
                            type: object
                          removeFromSet:
                            properties:
                              set:
                                description: Target set variable name
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              value:
                                description: Value to add (templated string allowed)
                                minLength: 1
                                type: string
                            required:
                            - set
                            - value
                            type: object
                          setVariable:
                            properties:
                              scalar:
                                description: Target set variable name
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              value:
                                description: Value to add (templated string allowed)
                                minLength: 1
                                type: string
                            required:
                            - scalar
                            - value
                            type: object
                        type: object
                      type: array
                    when:
                      description: When specifies the conditions under which the rule
                        is triggered.
                      properties:
                        alertName:
                          description: 'Variant 1: alert'
                          type: string
                        status:
                          type: string
                        updateType:
                          enum:
                          - added
                          - removed
                          - set
                          type: string
                        variableUpdated:
                          description: 'Variant 2: variable-driven'
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one variant must be set
                        rule: (has(self.alertName)) != (has(self.variableUpdated))
                  required:
                  - name
                  - then
                  - when
                  type: object
                type: array
              variables:
                items:
                  description: Variable defines mdai variable
                  properties:
                    dataType:
                      description: DataType Data type for the managed variable value
                      enum:
                      - string
                      - int
                      - set
                      - map
                      - boolean
                      - metaHashSet
                      - metaPriorityList
                      type: string
                    key:
                      description: Key The key for which this variable's managed value
                        is assigned. Will also be used as the environment variable name
                        for variables of type "string"
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                      type: string
                    serializeAs:
                      items:
                        properties:
                          name:
                            description: Name The environment variable name to be used
                              to access the variable's value.
                            minLength: 1
                            pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                            type: string
                          transformers:
                            description: |-
                              Transformers The transformation applied to the value of the variable in the order provided before it is assigned
                              as an environment variable.
                            items:
                              properties:
                                join:
                                  description: Join For use with "set" or "array" type
                                    variables, joins the items of the collection into
                                    a string.
                                  properties:
                                    delimiter:
                                      description: Delimiter The delimiter inserted
                                        between each item in the collection during the
                                        Join
                                      type: string
                                  required:
                                  - delimiter
                                  type: object
                                type:
                                  type: string
                              required:
                              - type
                              type: object
                            type: array
                        required:
                        - name
                        type: object
                      type: array
                    storageType:
                      default: mdai-valkey
                      description: StorageType defaults to "mdai-valkey" if not provided
                      enum:
                      - mdai-valkey
                      type: string
                    type:
                      default: computed
                      description: Type for the variable, defaults to "computed" if
                        not provided
                      enum:
                      - manual
                      - computed
                      - meta
                      type: string
                    variableRefs:
                      description: VariableRefs name references to other managed variables
                        to be included in meta calculation. Listed variables should
                        be of the same data type.
                      items:
                        type: string
                      type: array
                  required:
                  - dataType
                  - key
                  - storageType
                  - type
                  type: object
                  x-kubernetes-validations:
                  - rule: 'self.type == ''meta'' ? self.dataType in [''metaHashSet'',
                      ''metaPriorityList''] : self.dataType in [''string'', ''int'',
                      ''boolean'', ''set'', ''map'']'
                type: array
            type: object
          status:
            description: MdaiHubStatus defines the observed state of MdaiHub.
            properties:
              conditions:
                description: Conditions store the status conditions of the Cluster instances
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              hubStatus:
                description: Status of the Cluster defined by its modules and dependencies
                  statuses
                type: string
              lastUpdatedTime:
                description: |-
                  Time when last Cluster Configuration change was detected
                  Right now it's updated on each reconcile, we have to skip when reconciliation detects no changes
                format: date-time
                type: string
            required:
            - hubStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
{{- end }}
